# RTK — Rust Token Killer

Use `rtk` as the prefix for shell commands in this repository.

```bash
rtk git status
rtk npm run build
rtk ddev drush status
```

For commands whose output must not be filtered, use `rtk proxy <command>`.
