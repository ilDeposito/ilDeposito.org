name: 'ilDeposito Redirects'
description: 'Redirect legacy dal vecchio sito e report periodico dei 404, per la migrazione dal monolite Drupal 8.'
package: 'ilDeposito'
core_version_requirement: ^11 || ^12
type: module
dependencies:
  # Riusa GitHubWorkflowClient (GitHub App + trigger workflow_dispatch) per
  # il pulsante "Pubblica redirect" in RedirectsForm.
  - ildeposito_build
