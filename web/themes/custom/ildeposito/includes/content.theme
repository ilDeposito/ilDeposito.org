<?php

/**
 * @file
 * Theme and preprocess functions for nodes.
 */

function ildeposito_preprocess_node(array &$variables): void
{
  // Recupera l'entità node per evitare errori e permettere il debug
  $node = $variables['node'];

}