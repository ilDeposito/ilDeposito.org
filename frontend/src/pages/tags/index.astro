---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Breadcrumb from '../../components/ui/Breadcrumb.astro';
import ShareModal from '../../components/ui/ShareModal.astro';
import Icon from '../../components/base/Icon.astro';
import { Image } from 'astro:assets';
import { getTags, getContenutiByTagMap, getImageUrl } from '../../lib/api/index.js';
import { buildCollectionPageSchema, buildBreadcrumbSchema } from '../../lib/schema.js';
import { buildCanonical } from '../../lib/seo.js';
import { getPageMeta } from '../../lib/pages.js';

const tags = await getTags();
const contenutiMap = await getContenutiByTagMap();

const meta = getPageMeta('tags.index');
const siteUrl = Astro.site?.href?.replace(/\/$/, '') || 'https://www.ildeposito.org';
const jsonLd = [
  buildCollectionPageSchema(meta.metaTitle, meta.metaDescription, buildCanonical(Astro)),
  buildBreadcrumbSchema([
    { name: 'Home', url: siteUrl },
    { name: 'Tags' },
  ]),
];
---
<BaseLayout title={meta.metaTitle} description={meta.metaDescription} jsonLd={jsonLd}>

  <header class="bg-base-200 border-b-2 border-secondary">
    <div class="max-w-6xl mx-auto px-4 py-6 flex flex-col gap-3">
      <Breadcrumb items={[{ label: 'Home', href: '/' }, { label: 'Tags' }]} />

      <h1 class="font-serif font-bold text-2xl md:text-3xl text-base-content">{meta.pageTitle}</h1>

      <p class="font-serif italic text-base text-base-content/60">{meta.pageDescription}</p>

      <share-button data-title="Tags" class="contents">
        <button type="button" class="inline-flex items-center gap-1 cursor-pointer w-fit" aria-label="Condividi questa pagina">
          <Icon name="share-network" />
          Condividi
        </button>
        <ShareModal />
      </share-button>
    </div>
  </header>

  <div class="max-w-6xl mx-auto px-4 py-8">

    <div class="grid grid-cols-1 md:grid-cols-2 gap-x-8 gap-y-10 mt-6">
      {tags.map((tag, i) => {
        const { canti = 0, eventi = 0 } = contenutiMap.get(tag.id) ?? {};
        const immagineUrl = getImageUrl(tag.immagine);

        return (
          <div class="flex items-start gap-5 group relative">
            <div class="shrink-0">
              {immagineUrl ? (
                <Image
                  src={immagineUrl}
                  alt={"Immagine del tag: " + tag.titolo}
                  width={240}
                  height={240}
                  format="webp"
                  quality={80}
                  loading={i < 4 ? 'eager' : 'lazy'}
                  class="w-32 h-32 md:w-36 md:h-36 rounded-full object-cover border-2 border-base-300 grayscale group-hover:grayscale-0 transition-all duration-300"
                />
              ) : (
                <div class="w-32 h-32 md:w-36 md:h-36 rounded-full bg-base-200 flex items-center justify-center border-2 border-base-300">
                  <Icon name="funnel" class="text-base-content/30 text-5xl" />
                </div>
              )}
            </div>

            <div class="flex flex-col gap-2 min-w-0 pt-2">
              <h2 class="font-serif font-bold text-xl md:text-2xl text-primary leading-tight">
                <a href={`/tags/${tag.slug}`} class="hover:underline after:absolute after:inset-0">{tag.titolo}</a>
              </h2>

              <div class="flex flex-wrap gap-x-5 gap-y-1.5">
                {canti > 0 && (
                  <p class="text-lg text-base-content/60 flex items-center gap-2">
                    <Icon name="music-notes" class="text-base text-primary/70 shrink-0" />
                    <span class="font-bold text-base-content">{canti}</span> {canti === 1 ? 'canto' : 'canti'}
                  </p>
                )}
                {eventi > 0 && (
                  <p class="text-lg text-base-content/60 flex items-center gap-2">
                    <Icon name="calendar-star" class="text-base text-primary/70 shrink-0" />
                    <span class="font-bold text-base-content">{eventi}</span> {eventi === 1 ? 'evento' : 'eventi'}
                  </p>
                )}
              </div>
            </div>
          </div>
        );
      })}
    </div>

  </div>

  <script>
    import '../../scripts/share-button.js';
  </script>
</BaseLayout>
