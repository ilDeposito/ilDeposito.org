name: Build Frontend Content (Prod)

on:
  workflow_dispatch:

concurrency:
  group: build-frontend-prod
  cancel-in-progress: true

jobs:
  build:
    runs-on: self-hosted
    defaults:
      run:
        working-directory: /home/ubuntu/sergej/websites/ildeposito/prod

    steps:
      - name: Build frontend (solo contenuti, no pdf)
        run: ./ildeposito.sh build-frontend-content
