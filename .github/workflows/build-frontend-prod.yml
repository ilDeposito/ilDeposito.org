name: Build Frontend (Prod)

on:
  workflow_dispatch:

concurrency:
  group: build-frontend-prod
  cancel-in-progress: true

jobs:
  build:
    runs-on: self-hosted
    defaults:
      run:
        working-directory: /home/ubuntu/sergej/websites/ildeposito/prod

    steps:
      - name: Build frontend
        run: ./ildeposito.sh build-frontend
