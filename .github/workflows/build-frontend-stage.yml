name: Build Frontend (Stage)

on:
  workflow_dispatch:

concurrency:
  group: build-frontend-stage
  cancel-in-progress: true

jobs:
  build:
    runs-on: self-hosted
    defaults:
      run:
        working-directory: /home/ubuntu/sergej/websites/ildeposito/stage

    steps:
      - name: Build frontend
        run: ./ildeposito.sh build-frontend